stages:
    - build
    - test

build_sing:
    stage: build
    tags:
        - dbuilder_cc
    script:
        - whoami
        - pwd
        - ls
    
    artifacts:
        paths:
            - build_*
test:
    stage: test
    script: 
    - echo "echo testing"
    - pwd
