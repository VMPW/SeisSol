stages:
    - build
    - test

build:
    stage: build
    tags:
        - shell_exec
    before_script:
        - echo "before script"
        - whoami
        - pwd
        - ls
    script:
        - echo "script"
        - whoami
        - pwd
        - ls

    
    artifacts:
        paths:
            - build_*
test:
    stage: test
    script: 
    - echo "echo testing"
    - pwd
